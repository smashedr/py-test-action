name: "Python Test Action"
description: "Python Test Action."
author: "Shane"
branding:
  icon: "type"
  color: "green"

inputs:
  url:
    description: "URL with {version} in String"
    required: true

outputs:
  url:
    description: "Update URL"

runs:
  using: "docker"
  image: "Dockerfile"
